apiVersion: v1
kind: Secret
metadata:
  name: signaling-server-secrets
  namespace: quick-share
type: Opaque
data:
  # These are example values, replace with actual secrets in production
  # Values are base64 encoded
  REDIS_PASSWORD: ""  # base64 encoded password if needed